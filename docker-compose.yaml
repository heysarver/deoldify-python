version: "3.8"
services:
  deoldify-python:
    image: local/deoldify-python:dev
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./models:/app/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
